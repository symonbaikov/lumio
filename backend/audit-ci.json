{
  "$schema": "https://github.com/IBM/audit-ci/raw/main/docs/schema.json",
  "moderate": true,
  "allowlist": [
    {
      "GHSA-vwc7-r8mq-g2x9": {
        "active": true,
        "notes": "adm-zip <=0.6.0: extraction follows destination symlinks. No patched release exists upstream. Not reachable here: the affected APIs (extractAllTo/extractEntryTo) are never called. Consumers are backups/backup-archive.service.ts (getEntry/getEntries/getData/addFile/toBuffer), parsing/parsers/docx.parser.ts (readAsText), and application-settings/application-settings.service.ts extractZipBuffer, which validates entry paths and writes via fs.writeFile. Also reached transitively via onnxruntime-node. Re-review at expiry.",
        "expiry": "2026-12-10"
      }
    }
  ]
}
