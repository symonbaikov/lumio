/**
 * Revision of the no-warranty disclaimer users are asked to accept.
 *
 * Bump this whenever the wording changes in a way that alters what the user is
 * agreeing to. The value is stored alongside each acceptance, so the record
 * says which text was shown rather than merely that something was.
 *
 * Dated rather than numbered so the record is legible without a changelog.
 */
export const CURRENT_DISCLAIMER_VERSION = '2026-08-21';
